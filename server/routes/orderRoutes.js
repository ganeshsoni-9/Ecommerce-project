const r=require("express").Router();const c=require("../controllers/orderController");const{protect}=require("../middleware/authMiddleware");const roles=require("../middleware/adminMiddleware");r.use(protect);r.post("/",c.create);r.get("/mine",c.mine);r.get("/:id",c.get);r.patch("/:id/cancel",c.cancel);r.get("/admin/all",roles("ADMIN","MANAGER"),c.adminList);r.patch("/admin/:id",roles("ADMIN","MANAGER"),c.updateStatus);module.exports=r;