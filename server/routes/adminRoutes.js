const r=require("express").Router();const c=require("../controllers/adminController");const{protect}=require("../middleware/authMiddleware");const roles=require("../middleware/adminMiddleware");r.use(protect,roles("ADMIN"));r.get("/users",c.users);r.patch("/users/:id/role",c.setUserRole);r.get("/dashboard",c.dashboard);module.exports=r;