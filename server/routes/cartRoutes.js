const r=require("express").Router();const c=require("../controllers/cartController");const{protect}=require("../middleware/authMiddleware");r.use(protect);r.get("/",c.get);r.post("/add",c.add);r.post("/items",c.add);r.put("/:productId",c.update);r.patch("/:productId",c.update);r.patch("/items/:productId",c.update);r.delete("/:productId",c.remove);r.delete("/items/:productId",c.remove);r.delete("/",c.clear);module.exports=r;