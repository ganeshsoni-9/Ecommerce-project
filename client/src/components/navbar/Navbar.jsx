import {Link,useNavigate} from "react-router-dom";import {ShoppingCart,User,Search,Menu,X} from "lucide-react";import {useSelector,useDispatch} from "react-redux";import {logout} from "../../redux/slices/authSlice";import {useState} from "react";
export default function Navbar(){const [open,setOpen]=useState(false);const nav=useNavigate(),dispatch=useDispatch();const user=useSelector(s=>s.auth.user),count=useSelector(s=>s.cart.items.reduce((a,i)=>a+i.quantity,0));return <><div className="bg-slate-900 text-white text-center text-xs py-2">Free shipping on orders above ₹1,000</div><header className="sticky top-0 z-40 bg-white/95 backdrop-blur border-b"><div className="container-page h-16 flex items-center justify-between gap-4"><Link to="/" className="font-black text-xl tracking-tight">Commerce<span className="text-indigo-600">Scale</span></Link><nav className="hidden md:flex gap-6 text-sm font-semibold"><Link to="/">Home</Link><Link to="/shop">Shop</Link><Link to="/about">About</Link><Link to="/contact">Contact</Link></nav><div className="flex items-center gap-2"><button onClick={()=>nav("/search")} className="btn-light !p-2"><Search size={18}/></button><button onClick={()=>nav("/cart")} className="relative btn-light !p-2"><ShoppingCart size={18}/>{count>0&&<span className="absolute -top-1 -right-1 rounded-full bg-indigo-600 text-white text-[10px] px-1.5">{count}</span>}</button><Link to="/login" className="hidden sm:flex btn-light !py-2 !px-3 flex items-center gap-1.5 text-sm font-semibold"><User size={16}/> Account</Link><button className="md:hidden btn-light !p-2" onClick={()=>setOpen(!open)}>{open?<X size={18}/>:<Menu size={18}/>}</button></div></div>{open&&<div className="md:hidden border-t p-4 flex flex-col gap-3"><Link to="/shop">Shop</Link><Link to="/about">About</Link><Link to="/contact">Contact</Link><Link to="/login" onClick={()=>setOpen(false)}>Account</Link></div>}</header></>}
