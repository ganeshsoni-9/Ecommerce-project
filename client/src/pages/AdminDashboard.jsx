import {useEffect,useState} from "react";import {dashboard} from "../services/adminService";import StatCard from "../components/admin/StatCard";import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip} from "recharts";import api from "../services/api";
export default function AdminDashboard(){const[d,setD]=useState();const[a,setA]=useState([]);useEffect(()=>{dashboard().then(r=>setD(r.data.data));api.get("/analytics/dashboard").then(r=>setA(r.data.data.revenue)).catch(()=>{})},[]);if(!d)return <div>Loading dashboard…</div>;return <div><h1 className="text-3xl font-black">Dashboard</h1><div className="grid sm:grid-cols-2 lg:grid-cols-5 gap-4 mt-6"><StatCard label="Revenue" value={`₹${d.revenue}`}/><StatCard label="Orders" value={d.orders}/><StatCard label="Customers" value={d.customers}/><StatCard label="Products" value={d.products}/><StatCard label="Low stock" value={d.lowStock}/></div><div className="card p-5 mt-6 h-80"><h2 className="font-bold mb-4">Monthly revenue</h2><ResponsiveContainer width="100%" height="90%"><LineChart data={a}><XAxis dataKey="_id"/><YAxis/><Tooltip/><Line type="monotone" dataKey="revenue"/></LineChart></ResponsiveContainer></div></div>}
