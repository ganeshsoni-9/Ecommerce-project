import {useEffect,useState} from "react";import {users,updateRole} from "../services/adminService";import toast from "react-hot-toast";
export default function AdminUsers(){const[u,setU]=useState([]);const load=()=>users().then(r=>setU(r.data.data));useEffect(load,[]);const role=async(id,e)=>{try{await updateRole(id,e.target.value);toast.success("Role updated");load()}catch(x){toast.error(x.response?.data?.message||"Failed")}};return <div><h1 className="text-3xl font-black">Users</h1><div className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-4">Name</th><th>Email</th><th>Role</th></tr></thead><tbody>{u.map(x=><tr className="border-b" key={x._id}><td className="p-4">{x.name}</td><td>{x.email}</td><td><select className="input !w-auto !py-2" value={x.role} onChange={e=>role(x._id,e)}><option>CUSTOMER</option><option>MANAGER</option><option>ADMIN</option></select></td></tr>)}</tbody></table></div></div>}
