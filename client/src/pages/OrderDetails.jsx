import {useEffect,useState} from "react";import {useParams} from "react-router-dom";import {getOrder} from "../services/orderService";import Loader from "../components/common/Loader";
export default function OrderDetails(){const{id}=useParams(),[o,setO]=useState();useEffect(()=>getOrder(id).then(r=>setO(r.data.data)),[id]);if(!o)return <Loader/>;return <div className="container-page py-10"><h1 className="text-4xl font-black">{o.orderNumber}</h1><div className="card p-6 mt-7"><p>Status: <b>{o.orderStatus}</b></p><p className="mt-2">Payment: <b>{o.paymentStatus}</b></p><p className="mt-2">Total: <b>₹{o.totalAmount}</b></p><div className="mt-6 space-y-2">{o.items.map((i,n)=><div key={n} className="flex justify-between border-b py-2"><span>{i.name} × {i.quantity}</span><b>₹{i.price*i.quantity}</b></div>)}</div></div></div>}
